apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "edu-web-beta.fullname" . }}-test-connection"
  labels:
    {{- include "edu-web-beta.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "edu-web-beta.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
